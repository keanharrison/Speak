import { PlaceholderView } from "@/features/shared/PlaceholderView";
import { schedulingPage } from "@/lib/hardcoded-data";

export default function SchedulingPage() {
  return <PlaceholderView data={schedulingPage} />;
}
