import { PlaceholderView } from "@/features/shared/PlaceholderView";
import { settingsPage } from "@/lib/hardcoded-data";

export default function SettingsPage() {
  return <PlaceholderView data={settingsPage} />;
}
