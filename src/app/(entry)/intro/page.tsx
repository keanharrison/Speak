import { CinematicIntro } from "@/features/entry/CinematicIntro";

/** Cinematic intro — montage → TV → Speak. Before welcome splash. */
export default function IntroPage() {
  return <CinematicIntro />;
}
